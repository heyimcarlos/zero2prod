alerts:
- rule: DEPLOYMENT_FAILED
- rule: DOMAIN_FAILED
# ingress:
#   rules:
#     - component:
#         name: api
#       match:
#         path:
#           prefix: /
name: zero2prod
services:
- name: api
  dockerfile_path: Dockerfile
  source_dir: .
  github:
    branch: main
    deploy_on_push: true
    repo: heyimcarlos/zero2prod
  health_check: 
    http_path: /health_check
  http_port: 8000
  instance_count: 1
  instance_size_slug: basic-xxs
  routes:
    - path: /
